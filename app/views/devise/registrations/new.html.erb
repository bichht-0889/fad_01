<%= provide :button, t("devise.label.signup") %>

<section id="form">
  <div class="container">
    <div class="row">
      <div class="col-sm-4 col-sm-offset-4">
        <div class="login-form">
          <%= render "form" %>
          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</section>
